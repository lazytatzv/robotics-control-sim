services:
  app:
    image: node:22-bookworm-slim
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    command: sh -c "corepack enable && corepack prepare pnpm@11.3.0 --activate && pnpm install && pnpm dev --host 0.0.0.0"
